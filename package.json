{
  "name": "Color-Code-Export",
  "version": "1.0.0",
  "description": "Your Figma Plugin",
  "main": "code.js",
  "type": "module",
  "scripts": {
  "build": "esbuild code.ts --bundle --format=iife --platform=browser --target=es6 --outfile=code.js",
  "watch": "esbuild code.ts --bundle --format=iife --platform=browser --target=es6 --outfile=code.js --watch"
},
  "author": "Akshay Khurana",
  "license": "Free",
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "esbuild": "^0.25.8",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@material/material-color-utilities": "^0.3.0",
    "colorjs.io": "^0.4.5"
  }
}
